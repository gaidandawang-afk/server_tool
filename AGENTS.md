# Server Tool 协作规范

`server_tool` 用于在多人共享服务器上准备运行环境、构建源码、运行服务、
执行测试和定位失败。模型负责任务理解与能力选择；工具只负责确定性远程执行。

## 开始工作前

1. 运行 `git status --short --branch`，不得覆盖或混入他人的未提交更改。
2. 根据任务读取对应 `skills/<name>/SKILL.md`，只加载该 skill 要求的引用。
3. 若要调整本项目架构、目录、skill 边界、profile 模型或远程执行接口，必须先完整读取 `GOD.md`。
4. 开始远程操作前重新检查服务器目录、端口、GPU、容器和进程，不把历史状态当成当前事实。

## 始终适用的边界

- 本地已提交代码是事实来源；禁止在远端手改受版本控制的项目代码。
- 多人服务器上不得修改、复用或清理他人的文件、容器、端口、环境和进程。
- 共享 GPU 前必须取得用户针对本次运行的明确授权。GPU 评审要求每张所选 GPU 有超过
  30 GiB 可用显存；已有 compute 进程不再作为拒绝条件，但必须保留其 PID/显存信息，且
  不得清理或控制既有进程。`--allow-busy-gpus` 保留为显式授权的兼容性确认项。
- 本项目远端持久化边界是 `/data2/iws`；镜像归档只能放在 `/data1/images`。
- 密钥、密码、令牌、本地 profile 和其他秘密不得提交。
- 成功和失败都必须留下提交号、命令、状态、日志及可取回的 artifact。

## 环境与任务

共享基础运行环境只包括一次安装后供所有任务使用的不变部分：

- Python、CUDA 和基础依赖库；
- 稳定安装的 Mooncake；
- pip、wheel、模型和编译缓存；
- 固定的共享环境加载脚本及路径。

从待测源码开始都属于具体 task，包括源码 worktree、构建目录、服务进程、
任务变量、GPU、端口、测试脚本、日志、临时目录和 artifact。

- 一个逻辑 task 使用一个平坦 profile。
- 每个 profile 独占任务根目录、远端源码 worktree、端口段和 artifact 根目录。
- GPU 由 profile 显式指定，默认可为 `all`；只检查和使用该 profile 选择的 GPU。
- 端口由 `PORT_BASE` 和 `PORT_COUNT` 显式指定；冲突必须快速失败，不做自动调度。
- 每个 task 自己启动、观察并清理服务，不引入额外的服务所有权概念。
- ABI 敏感或需要并存的 Python 包安装在显式版本目录；目录创建后不可覆盖。
- profile 直接选择依赖目录，不建立 `current`、`latest`、全局激活状态或隐式回退。
- 启动前验证实际包版本、模块路径和必要符号，证明导入来自 profile 选择的目录。

## 源码与分支

- profile 可以引用具体本地和远端代码分支。
- 执行时从本地分支 HEAD 得到期望提交，不在 profile 或用例中硬编码提交哈希。
- 普通验证可使用指定功能分支，但远端始终使用当前 task 的独立 worktree。
- 需要修改 SGLang、Mooncake 或其他待测源码时，创建独立本地 worktree 和临时 debug 分支。
- 临时 debug 分支优先命名为 `codex/debug/<profile>-<issue>`。
- instrumentation 和修复必须先在本地提交，再同步相同提交到远端验证。
- 调试结束后不擅自删除分支、worktree、远端目录或 artifact。

## 远程执行

- 非平凡远程操作写成明确输入的脚本，再交给远程执行工具运行。
- 长任务必须与 SSH 文件描述符完全脱离，通过短连接执行 `status`、`logs`、`wait` 和 `fetch`。
- 所有等待必须有界；SSH 断开不能导致任务或证据消失。
- 只清理当前 task 记录且验证所有权的 PID/PGID，禁止宽泛匹配进程。
- 不得杀死或重启未明确属于当前 task 的服务。
- run 输入、临时工作和输出应分离；只取回 output、必要控制文件和小型 provenance。

## 测试与调试

- 测试契约声明适用源码分支，只提交已经执行或明确要求固化的稳定用例。
- 稳定测试契约提交在 `skills/test-service/cases/`，并声明适用的待测源码分支。
- 外部项目只能作为设计参考；执行规则、脚本、oracle 和结果不得依赖外部测试项目。
- 一个用例由 `TEST.md + run.sh` 表达：前者定义意图和通过条件，后者只做机械执行。
- 只并行同一阶段内明确独立的请求；存在因果关系的阶段必须保留 barrier。
- 只有具体测试用例已经失败并进入根因定位时，`debug-service` 才能加载经验池。
- 经验池不能替代本次日志和 artifact；验证修复后仍需用户确认才能更新经验池。
- `TEST.md` 必须声明阶段状态、接口断言、进程断言、精度标准、重复方式和必要 artifact。
- 每次冷启动使用独立 run 名称和 artifact 目录，禁止后一次覆盖前一次。

## 文件归属

新增脚本前必须先确定其唯一所有者：

- 通用 SSH、SFTP、任务状态和 artifact 机制属于 `tools/`。
- 稳定的领域叶子操作属于对应 skill 的 `scripts/`。
- 镜像配置和生命周期操作属于 `skills/manage-image/`。
- 稳定测试契约属于 `skills/test-service/cases/`；待测仓库只保存产品源码自己的测试。
- 一次性实验、调试包装和临时用例属于忽略的 `work/<profile>/<task>/`。

禁止使用根级 `scripts/` 作为收纳目录。单次事故形成的命令不得直接提升为永久脚本。

## Git 与临时文件

1. 所有仓库文件更新都必须通过 Git 提交保存。
2. 每个提交只包含一个明确目的，提交信息必须说明更改原因和验证背景。
3. 提交前检查暂存区、运行相关验证并执行 `git diff --check`。
4. 所有 task-local、生成物和临时文件统一放在 `work/`，该目录不得提交。
5. 只清理由当前任务创建且已确认路径的临时内容。
