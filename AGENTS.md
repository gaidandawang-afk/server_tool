# Agent 项目协作规范

## 目录约定

- `agents/`：Agent 定义与编排配置。
- `prompts/`：可复用的提示词模板。
- `skills/`：Agent 技能及其配套资源。
- `tools/`：Agent 可调用的工具实现。
- `scripts/`：开发、检查和发布脚本。
- `tests/`：自动化测试。
- `docs/`：设计说明与使用文档。
- `.tmp/`：本地临时文件，禁止提交。

## Git 工作规范

1. 所有文件更新都必须纳入 Git 版本控制，并通过提交保存。
2. 开始修改前检查 `git status`，不得覆盖或混入他人的未提交更改。
3. 每个提交只包含一个明确目的，避免混入无关调整。
4. 提交信息必须说明更改原因，而不只是描述改了什么。
5. 提交前检查暂存区和差异，并运行与改动相关的验证。
6. 禁止提交密钥、令牌、密码、私有证书及其他敏感信息。

## 临时文件规范

1. 所有临时文件统一存放在仓库根目录的 `.tmp/` 中。
2. `.tmp/` 由 `.gitignore` 排除，目录内容不得提交。
3. 不得在源码、测试、文档等目录中散落临时文件。
4. 新增其他本地生成物时，应先将其规则加入 `.gitignore`，再执行生成操作。
