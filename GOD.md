# Server Tool 设计与优化规则

本文件只指导 `server_tool` 自身的架构演进。普通镜像构建、环境安装、源码编译、
服务运行和测试不需要读取本文件。

## 必须先读取本文件的改动

- 调整根目录结构；
- 新增、合并、拆分或重命名 skill；
- 修改 skill、工具、profile 或 task 的职责边界；
- 修改远程任务生命周期、进程清理或 artifact 模型；
- 修改 `tools/` 中远程执行接口或安全模型；
- 引入共享状态、执行层、调度、编排或其他架构抽象；
- 对本项目进行整体设计优化。

## 项目定位

`server_tool` 是服务于一台多人共享 GPU 服务器的小型操作 agent。

- 模型理解用户目标、选择 skill、组织步骤、分析证据并决定下一步。
- 工具执行 SSH、SFTP、后台任务、状态检查和 artifact 获取等确定性操作。
- skill 保存行为原则、领域知识、按需引用和稳定叶子脚本。
- task-local 文件保存一次性实验和本次执行证据。

任何优化都必须保持这三层边界，不得用程序规定模型的完整工作流。

## 不变量

1. 本地已提交源码是唯一代码事实来源，远端不手改。
2. 基础环境与 task 资源严格分离。
3. 一个 task 一个 profile，不建立环境 ID、版本指针或资源调度器。
4. 每个 task 负责自己的服务全生命周期。
5. 长任务采用无 daemon 的后台 runner 和短连接有界观测。
6. 进程清理必须基于当前 task 记录并验证所有权的 PID/PGID。
7. 可复现测试契约由本项目提交保存，并显式声明适用源码分支；不得依赖参考项目或临时目录。
8. 失败和成功都必须产生可解释、可取回的证据。

## 优化判断

新增目录、模块、skill 或脚本前，必须回答：

1. 当前可复现的问题是什么？
2. 为什么已有边界不能解决？
3. 谁唯一拥有新增内容？
4. 这是重复出现的稳定能力，还是一次性任务？
5. 是否可以通过删除、移动、合并或参数化现有内容解决？
6. 最小变更是什么？
7. 哪些旧入口会被删除？
8. 如何验证没有破坏现有安全边界？

单次事故、单个版本或一台服务器上的偶发现象不足以产生新抽象。

## 禁止方向

- daemon 或常驻控制服务；
- GPU、端口或任务自动调度器；
- 程序化 workflow、状态图或强制 skill 调用顺序；
- skill 间 manifest/schema 协议；
- subagent 编排框架；
- 通用云平台、target adapter 或资源租约框架；
- 共享环境版本、`current` 指针或“环境修复”流程；
- 根级脚本垃圾抽屉；
- 为未经要求的兼容性保留旧入口。

测试结果的最小证据清单不属于 skill 间协议；它是 `test-service` 对每次独立运行
的验收边界。通用 runner 只收集输入、生命周期和 artifact，不解释测试场景。

## 架构变更流程

1. 先核对物理仓库状态并列出受影响文件。
2. 明确现状、目标、非目标和可验证成功条件。
3. 优先删除无职责内容，再移动错位内容，最后才新增必要抽象。
4. 每一处改动都必须能追溯到已确认的问题。
5. 更新对应 `AGENTS.md`、`SKILL.md` 或 README，避免规则只存在于对话。
6. 运行单元、语法、路径和安全门禁验证。
7. 检查差异并创建说明原因与验证背景的单一目的提交。
